window.onload = () => {
    const imgUrl = "https://dog.ceo/api/breeds/image/random/4"

    fetch(imgUrl).then(response => {
        return response.json()
    }).then(json => {
        document.getElem
    })
}